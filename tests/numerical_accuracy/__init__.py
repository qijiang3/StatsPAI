"""Numerical-accuracy certification suite.

Certifies StatsPAI's numerical kernels against published, multiple-precision
*certified* reference values (NIST StRD), as distinct from the cross-language
R/Stata recovery fixtures in ``tests/reference_parity/``. Kept out of the
JSS-headlined ``reference_parity`` count on purpose — see ``README.md``.
"""
